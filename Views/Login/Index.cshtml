﻿@model AgenziaCheSpedisce.Models.User

@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.Username)
    @Html.EditorFor(model => model.Username)

    @Html.LabelFor(model => model.Username)
    @Html.EditorFor(model => model.Username)

    <button class="btn btn-primary">Login</button>
}

